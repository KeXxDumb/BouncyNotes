<?xml version="1.0" encoding="utf-8"?>
<!-- Vector vacío (sin paths): deja que la capa de fondo (el PNG del
     personaje) se vea completa en el ícono adaptativo, sin nada dibujado
     encima. Revertido a este estado neutro después de un intento de
     rediseño vectorial que quedó a medio camino — el rediseño real de
     Note Girl queda pendiente hasta tener el SVG que el usuario va a
     dibujar (ver conversación: AutoDraw / Vector Ink en el celular). -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108" />
